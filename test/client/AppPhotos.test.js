import React from 'react';
import renderer from 'react-test-renderer';
import AppPhotos from '../../client/src/components/AppPhotos.jsx';


describe('AppPhotos should contain a state object and necessary methods', () => {
  test('AppPhotos state should contain all necessary properties', () => {
    expect('something').toEqual(AppPhotos)
  })
})